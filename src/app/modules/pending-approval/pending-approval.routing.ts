import { Routes } from '@angular/router';
import { PendingApprovalContainer } from './containers/pending-approval/pending-approval.container';

export const PendingApprovalRoutes: Routes = [
    {
        path: '',
        component: PendingApprovalContainer,

    }
]